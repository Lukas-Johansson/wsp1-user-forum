{% extends "layout.njk" %}
{% block content %}
<div class="hero">
    <div class="cards">

    </div>
    <div class="senaste">
        <div class="catagory">
            <h2>Senaste upplägg</h2>
        </div>
        <ul>
            {% for row in rows %}
            <li>
                <div class="msg">
                    <a class="stretched-link" href="/post/{{ row.id }}">{{ row.title }}</a>
                    <div class="msg-title">
                        <p>{{ row.name }}</p>
                    </div>
                    <div class="msg-content">
                        <p>{{ row.content }}</p>
                        <p>{{ row.createdAt }}</p>
                    </div>
                </div>
            </li>
            {% endfor %}
        </ul>
    </div>
</div>


{% endblock %}
